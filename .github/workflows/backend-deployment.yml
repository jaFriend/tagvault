name: Azure VM CI/CD

on:
  push:
    branches:
      - main
    paths: ['backend/**']
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main
    paths: ['backend/**']
env:
  REMOTE_HOST: api.tagvault.cc
  REMOTE_USER: tagvault

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - uses: docker/setup-buildx-action@v3
      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: jafriend/tagvault-backend:latest
      - name: Deploy to Remote Server via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ env.REMOTE_HOST }}
          username: ${{ env.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker pull jafriend/tagvault-backend:latest
            docker stop tagvault-backend || true
            docker rm tagvault-backend || true
            docker run -d \
              --name tagvault-backend \
              --env-file .env \
              -v /home/tagvault/clerk_key.pem:/app/config/clerk_key.pem \
              -p 5050:5050 \
              jafriend/tagvault-backend:latest
